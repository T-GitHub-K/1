CODE	SEGMENT
	ASSUME	CS:CODE

ORG	100H
	
START:	MOV	BH,20

A:	PUSH	BX
	MOV	CL,78H
	MOV	AX,800
	MOV	DX,400
	MOV	CH,0
	INT	80H

	CALL	WAIT1
	CALL	WAIT1
	CALL	WAIT1
	CALL	WAIT1

	MOV	CL,78H
	MOV	AX,0F000H
	MOV	DX,150
	MOV	CH,0
	INT	80H

	CALL	WAIT1

	MOV	CL,78H
	MOV	AX,800
	MOV	DX,40
	MOV	CH,0
	INT	80H

	CALL	WAIT1

	MOV	CL,78H
	MOV	AX,0F000H
	MOV	DX,40
	MOV	CH,0
	INT	80H

	CALL	WAIT1

	MOV	CL,78H
	MOV	AX,800
	MOV	DX,40
	MOV	CH,0
	INT	80H

	CALL	WAIT1

	MOV	CL,78H
	MOV	AX,0F000H
	MOV	DX,150
	MOV	CH,0
	INT	80H

	CALL	WAIT1
	CALL	WAIT1
	CALL	WAIT1
	CALL	WAIT1
	CALL	WAIT1
	CALL	WAIT1
	CALL	WAIT1
	CALL	WAIT1

	POP	BX
	DEC	BH
	OR	BH,BH
	JNZ	A

	MOV	AH,4CH
	INT	21H


WAIT1	PROC
	MOV	BX,0F000H
L1:	DEC	BX
	OR	BX,BX
	JNZ	L1
	RET
WAIT1	ENDP

CODE	ENDS
	END	START
