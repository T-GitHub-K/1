PARA_OP	STRUC				;スタック受渡テーブル
	BP_SS	DW	?		;BPの退避
	R_ADD	DD	?		;RETURN	ADD
	DISP_P	DW	?		;DISP	ADD
PARA_OP	ENDS

CODE	SEGMENT
	ASSUME	CS:CODE,DS:CODE,SS:CODE,ES:CODE

	PUBLIC	DISP

DISP	PROC	FAR
	PUSH	BP
	MOV	BP,SP
	
	MOV	DX,[BP].DISP_P
	MOV	AH,09
	INT	21H

	POP	BP
	RET	2
DISP	ENDP

CODE	ENDS
	END
